
const webpack = (options, callback) => {
  const create = () => {};
  if(callback) {
    const { compiler } = create();
    return compiler;
  } else {
    const { compiler } = create();
    return compiler;
  }
};